# 融合前端交接上下文

本机权威目录：C:/Projects/AstraInspect/product/astra-inspect；主开发分支 main。使用交接时实际 git rev-parse HEAD 结果建立独立 worktree，建议分支 frontend/refine；本轮未启动外部模型或创建前端工作区。

真实路由：/ 总览、/inspection 三维、/playback 回放、/equipment 原演示设备、/review 问题复核、/integration 原接入、/instruments 新设备证据与三通道。新页直接请求 /api/instruments；访问授权也在此页。缺失的三维点选联动、自动站点转换不得画成已完成。示例来源仅 data/demo 与 data/contract-fixtures，全部为公开演示/契约夹具。

先读 README、PROGRESS、docs/development/INSTRUMENT_API.md、实际路由与 client.ts。只编辑 apps/web 下界面；接口/鉴权/车端由 Codex 负责。先给方案再实现，不建立静态替代站，不调用测试。构建见 docs/development/BUILD.md。

以下保留两阶段工作要求，方括号应以分支创建时的真实状态替换：

# Kimi / Gemini 前端接手提示词

使用时机：Codex 已形成统一仓库，核心接口和客户流程可供阅读。使用融合仓库的实际分支，不使用旧 digital-twin main。由 Codex 填入真实路径、分支、启动方式和页面入口；没有看过的截图不要声称看过。

选择一个模型主做实现；另一个可审阅方案，或在独立分支给备选方案。不要让两者同时编辑同一工作区。传入脱敏代码与示例，不传客户备份、密钥、私有现场照片。

## 第一阶段：设计方案

复制以下提示，并补全方括号：

> 你是 Field Inspect的产品界面设计师。项目是面向客户和操作人员的巡检证据与数字孪生平台，覆盖任务记录、空间回放、站点证据、设备关联、三通道读数和分析报告。品牌不绑定具体底盘。
>
> 真实融合仓库位于 [本机实际路径]，分支/commit 是 [版本]。先阅读项目约束、apps/web、实际路由、已有组件样式及 API 契约，列出你读到的入口。不要假设页面或接口已经实现。
>
> 请先提出一个可在现有框架内落地的视觉方案，不改代码：给出信息架构、主工作区布局、字体层级、颜色与间距规则，以及两个关键页面的布局说明。重点是专业、清晰、信息密度合理，突出场景、证据和分析；避免用大量发光、渐变和巨幅装饰挤占实际内容。
>
> 客户路径是：选择项目/场景 → 导入巡检包 → 查看任务与异常 → 空间回放并定位站点证据 → 查看设备关联及读数趋势 → 导出结果。根据实际已有能力设计，不虚构实时遥控或云端持续上传。
>
> 地图/三维、列表和证据详情需关联；三条读数曲线用可区分的颜色及文字标识。清楚区分实录、演示、缺失和待确认数据。覆盖加载、空内容、错误、无权限、导入失败和数据不完整状态。保持中文可读性、键盘操作、对比度和不同窗口宽度的可用性。
>
> 不引入新的 UI 框架或复杂设计系统，优先复用已有组件。指出会影响功能或需要新增接口的事项，交由 Codex 协调。此阶段不运行测试，不把设计效果当作已实现功能。

## 第二阶段：落地实现

选定方案后，给同一模型这段提示：

> 按已选定的 [方案名称/说明]，在 [独立分支或 worktree 路径] 实现 Field Inspect 的真实融合前端。先核对当前 commit 和他人修改，保留已有工作。
>
> 你负责 apps/web 内的页面布局、组件、样式和交互呈现。遵守仓库现有规范；跨目录改动、数据契约或服务端需求先写入交接清单，由 Codex 处理。不得改变 API 字段、状态机、业务判定、鉴权与导入/验收语义来迁就界面。
>
> 保留实际路由、API 请求、状态管理、场景及轨迹渲染、证据关联、读数趋势、下载导出功能。可以优化导航与组件组织，但必须接回真实功能；不要生成独立的静态展示站，不要用随机数字和假成功状态替代后端。尚未实现的能力采用诚实的空态或缺失提示。
>
> 优先完成：总览与导航、巡检任务与导入、空间回放与站点证据、设备详情与三通道趋势、报告和设置。以实际路由为准，缺失业务能力汇报给 Codex，不自行创造另一套后端。演示素材须有来源且与实录明确区分。
>
> 使用统一的颜色、字体层级、间距和控件状态；保持语义化 HTML、键盘可操作性、焦点提示、图表文字说明与足够对比度。三维视图与详情面板在常用桌面窗口宽度下都应可用。避免新增依赖，除非现有工具确实无法实现必要功能。
>
> 用户要求本阶段跳过测试：不运行单元/E2E/自动视觉测试；保留原有测试。必要构建可以执行，但先确认没有串联测试，不声称已经通过功能或视觉验收。
>
> 最后提交改动，说明分支/commit、改动文件、采用的设计规则、保留的功能连接、新增接口需求与未完成项。把可供 Codex 合并的代码和简短交接说明作为结果。不要自行公开私有材料或发布线上站点。

## Codex 合并要求

核对修改范围与 API 契约后合并选中的前端分支；另一方案仅留作参考。保持“代码完成”和“测试/客户验收完成”分别记录。向外部模型提供的材料应来自脱敏工作区，而不是整个 iCloud 项目。


### 最新实现边界：站点与三维联动

TaskPlaybackPage 已接入 RunStationPanel 和 RunAssetEvidence/useRunAssets。站点列表可跳转结果时间；同一 scene_version_id 的台账设备可选中并冻结查询时刻。保留“路线完成待复核”“区间不是驻留时长”“离线不是故障”等准确提示。站点采集元数据使用 GET /api/instruments/records/station_evidence?run_id=...；没有图片下载 API。禁止为展示效果补造设备、轨迹、读数或已确认状态。测试仍未执行。


StationEvidenceDetails 已连接真实 station_evidence / processing_report 和单次采集 process API，包含 blocked、observed_2d、no_configured_marker_detected 状态。优化页面时保留具体输入缺口、路线/采集序号区别、无三维定位和未校准置信度提示；不要把二维观测美化成已匹配设备。
