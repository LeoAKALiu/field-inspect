# SQLite path (defaults to services/api/twin.db; use :memory: or a tmp path for tests)
# TWIN_DB_PATH=services/api/twin.db

# Simulation tick period in milliseconds (default 1000)
# TWIN_SIM_TICK_MS=1000

# Simulation random seed (default 42)
# TWIN_SEED=42
