# apps/web

React + Vite shell. Feature pages under `src/pages/`, `src/components/`, etc. are reserved for the frontend agent.

Scaffold builds exclude those directories from TypeScript/ESLint until the frontend agent wires them into routing.
